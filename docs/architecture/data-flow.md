# Data Flow & State Mutation Diagram

> Generated by Graphify on 2026-08-11. Data transformations, query parameter routing, and WebGL telemetry streams.

## Diagram

```mermaid
flowchart LR
  classDef async fill:#1a1a2e,stroke:#e94560,color:#fff
  classDef error fill:#2d0000,stroke:#ff4444,color:#fff
  classDef store fill:#0d2137,stroke:#4ecdc4,color:#fff

  %% Inputs
  URL_PARAM([URL Query Param ?utm_persona=]) --> PARSE_UTM{UTMRouter.parse}
  USER_CLICK([Navbar Persona Tab Click]) --> SET_PERSONA[UTMRouter.setPersona]
  STORAGE_LOAD[(localStorage persona)]:::store --> HYDRATE{Hydrate State}

  %% Transformations
  PARSE_UTM -->|valid: patient/nurse/clinician| PERSIST[(Save to localStorage)]:::store
  PARSE_UTM -->|invalid/empty| DEFAULT_PERSONA[Default: clinician]
  SET_PERSONA --> PERSIST
  HYDRATE --> DISPATCH_EVENT[Dispatch Window CustomEvent 'utm_persona_changed']

  PERSIST --> DISPATCH_EVENT
  DEFAULT_PERSONA --> DISPATCH_EVENT

  %% UI Reaction
  DISPATCH_EVENT --> UPDATE_DOM[Update DOM Persona Badges & Viewports]
  DISPATCH_EVENT --> UPDATE_SHADERS[Re-theme WebGL Color Tokens]:::async

  %% WebGL Telemetry Loop
  RAF_LOOP([window.requestAnimationFrame]):::async --> UPDATE_PARTICLES[Update Particle Coordinates]
  UPDATE_PARTICLES --> RENDER_FRAME[WebGLRenderer.render]
  RENDER_FRAME --> CANVAS_OUT([HTML Canvas Render Output])

  %% Error & Disruption Handling
  CONTEXT_LOST[webglcontextlost Event]:::error --> STOP_RAF[Cancel Animation Frame Loop]
  CONTEXT_RESTORED[webglcontextrestored Event]:::async --> RESTART_RAF[Re-start Animation Frame Loop]
```

## Analysis Notes

- **Persona Routing Stream:** Client-side URL parameters and header clicks write to `localStorage` and trigger window events for reactive DOM and theme updates without full page reloads.
- **WebGL Frame Loop:** Non-blocking async rendering loop bound to container resize and WebGL context restoration events (`webglcontextlost`, `webglcontextrestored`).
- **Storage Safety:** `UTMRouter` checks `typeof window !== 'undefined'` to safely operate under Astro SSR compilation.
